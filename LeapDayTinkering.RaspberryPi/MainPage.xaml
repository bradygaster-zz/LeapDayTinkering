<Page
    x:Class="LeapDayTinkering.RaspberryPi.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LeapDayTinkering.RaspberryPi"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="140"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <TextBlock Text="Your Subscriber ID:" Grid.Column="0" Grid.Row="0" VerticalAlignment="Center"/>
        <TextBox Text="{Binding SubscriberId, Mode=TwoWay}" Grid.Column="1" Grid.Row="0" VerticalContentAlignment="Center" VerticalAlignment="Center"/>
        <ToggleButton IsChecked="{Binding IsSending, Mode=TwoWay}" Content="Start Sending" Grid.Row="1" Grid.Column="1"/>
    </Grid>
</Page>
